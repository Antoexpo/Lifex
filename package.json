{
  "name": "lifex",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace apps/api run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:all": "concurrently -n api,web \"npm run dev:api\" \"npm run dev:web\"",
    "build": "npm --workspace apps/api run build && npm --workspace apps/web run build",
    "start": "npm --workspace apps/api run start && npm --workspace apps/web run preview",
    "seed": "node prisma/seed.js",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vitest": "^1.3.0",
    "@vitest/coverage-v8": "^1.3.0"
  }
}
